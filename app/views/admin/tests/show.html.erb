<h1><%= t('.header', title: @test.title, category: @test.category.title) %></h1>


  <%= link_to t('shared.links.new_question'), new_admin_test_question_path(@test) %>

<h2><%= t('.header2') %>:</h2>

  <table>
  <thead>
  <tr>
    <th><%= t('.question_body') %>:</th>
    <th colspan="3"><%= t('.actions') %></th>
  </tr>
  </thead>

  <%= render partial: 'admin/questions/question', collection: @test.questions %>
  </table>

  <%= link_to t('shared.links.back'), admin_tests_path(@test) %>

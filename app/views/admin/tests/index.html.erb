<h1><%= t('.header') %></h1>
<p>
  <%= link_to t('shared.links.badges_list'), admin_badges_path %>
</p>
<p>
  <%= link_to t('shared.links.gists_list'), admin_gists_path %>
</p>
<p>
  <%= link_to t('shared.links.new_test'), new_admin_test_path %>
</p>

<%= render 'shared/errors', resource: @test %>
<table class="table table-sm">
  <tbody>
    <tr class="table-primary">
      <th scope="col" class="sort-by-title pointer text-center">
        <%= t('.title') %>
        <%= octicon 'arrow-up', class: 'text-success hide' %>
        <%= octicon 'arrow-down', class: 'text-success hide' %>
      </th>
      <th scope="col" class="text-center"><%= t('.level') %></th>
      <th scope="col" class="text-center"><%= t('.category') %></th>
      <th scope="col" class="text-center"><%= t('.questions') %></th>
      <th scope="col" class="text-center"><%= t('.author') %></th>
      <th scope="col" class="text-center" colspan="3"><%= t('.actions') %></th>
    </tr>
      <%= render @tests  %>
  </tbody>
</table>

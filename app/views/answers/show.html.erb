<h1><%= @answer.question.test.title %></h1>

<p>
  <%= @answer.question.body %>
</p>

<h2>Answers</h2>

<ol>
  <%# <%= render @answer.body %> %>
</ol>

<%= link_to 'New answer', new_question_answer_path(@answer) %>
<%= link_to 'Edit', edit_question_path(@answer) %>
<%= link_to 'Back', test_questions_path(@answer) %>

<nav class="navbar navbar-light bg-light">
  <%= link_to "Test Guru", root_path, class: 'navbar-brand' %>

  <span class="navbar-text">
    <% if user_signed_in? %>
      <%= t('shared.nav.welcome') %><b><%= current_user.first_name %></b><%= t('shared.nav.guru') %> |
      <%= link_to t('shared.links.log_out'), destroy_user_session_path, method: :delete %>
    <% else %>
      <%= link_to t('shared.links.log_in'), new_session_path(resource_name) %> |
      <%= link_to t('shared.links.sign_up'), new_registration_path(resource_name) %>
    <% end %>
  </span>
</nav>

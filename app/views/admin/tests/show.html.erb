<h1>Test: <%= @test.title %> | Category: <%= @test.category.title %></h1>

  <%= link_to 'New question', new_admin_question_path(@question) %>

<h2>Questions:</h2>

  <table>
  <thead>
  <tr>
    <th>Question body</th>
    <th colspan="3">Actions</th>
  </tr>
  </thead>

  <%= render partial: 'admin/questions/question', collection: @test.questions %>
  </table>

  <%= link_to 'Edit', edit_admin_question_path(@test) %>
  <%= link_to 'Back', admin_tests_path(@test) %>

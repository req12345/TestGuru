
<%= link_to t('shared.links.badges_existing'), all_badges_badge_path(current_user.id) %>
<h2 class="display-4"><%= t('.header') %></h2>

<table class="table table-hover">
  <tr class="table-primary">
    <th class="align-middle"><%= t('.title') %></th>
    <th class="align-middle"><%= t('.img_url') %></th>
    <th class="align-middle"><%= t('.recieved_at') %></th>
  </tr>

  <%= render current_user.badges  %>
</table>

<%= link_to t('shared.links.back'), root_path %>
